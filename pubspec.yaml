name: boldask
description: Boldask - Community engagement platform for polls, circles, and connections.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0
  firebase_storage: ^11.6.0

  # State Management
  provider: ^6.1.1

  # Routing
  go_router: ^11.1.4

  # UI
  google_fonts: ^4.0.4
  http: ^0.13.6
  cached_network_image: ^3.2.3
  flutter_svg: ^2.0.9

  # Utilities
  intl: ^0.18.1
  geolocator: ^10.1.0
  image_picker: ^1.0.4
  url_launcher: ^6.1.11
  collection: ^1.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/backgrounds/

  fonts:
    - family: BoldaskIcons
      fonts:
        - asset: assets/fonts/BoldaskIcons.ttf
